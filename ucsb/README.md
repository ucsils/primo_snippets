[UCSB's gitlab repository](https://gitlab.com/ucsb-library/alma-primo)
- contains all `html`, `css`, and javascript files used in primo view
- captures custom normalization rules for local search and display fields
- uses `docker compose` for local development
- includes `helm` chart for deploying review applications to kubernetes 
